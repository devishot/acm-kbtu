<div id="body-bgcolor">
  <p>
    <b>Name:</b>
    <%= @contest.name %>
  </p>
  <p>
    <b>Description:</b>
    <%= @contest.description %>
  </p>
  <p>
    <b>Start:</b>
    <%= @contest.time_start.strftime("%Y-%m-%d %H:%M") %>
  </p>
  <p>
    <b>Server time:</b>
    <%= 0.hours.from_now.strftime("%Y-%m-%d %H:%M:%S") %>
  </p>
  <p>
    <b>Finish:</b>
    <%= @contest.time_finish.strftime("%Y-%m-%d %H:%M") %>
  </p>

  <% if (current_user.participants.where(contest: @contest).count == 0) then %>
    <%= link_to "Participate", contest_path(@contest.path) + "/participate" %>
  <% end %>

  <ul class="nav nav-pills">
    <li <%= "class=active" if @navpill==1 %> > 
      <%= link_to "Problems", "/contests/#{@contest.path}/problems" %>
    </li>

    <% if @contest.problems_type == 1 %>    
      <li> 
        <%= link_to "Statements", "/contests/#{@contest.path}/problems/statement.pdf", :target => "_blank" %>
      </li>
    <% end%>

    <li <%= "class=active" if @navpill==2 %> > 
      <%= link_to "Standings", "/contests/#{@contest.path}/standings" %>
    </li>

    <li <%= "class=active" if @navpill==3 %> > 
      <%= link_to "Messages", "/contests/#{@contest.path}/messages" %>
    </li>

    <li <%= "class=active" if @navpill==4 %> > 
      <%= link_to "Summary", "/contests/#{@contest.path}/summary" %>
    </li>
  </ul>

</div>