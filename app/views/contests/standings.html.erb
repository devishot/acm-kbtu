<%= render "contest" %>

<table class="table table-striped table-bordered table-hover table-condensed" >
  <thead>
    <tr>
      <th>№</th>
      <th>Name</th>
      <% for p in 1..@contest.problems_count %>
        <th><%= p %></th>
      <% end %>
      <th>All</th>
      <th>Penalty</th>
    </tr>
  </thead>

  <tbody>
    <% @contest.participants.each do |participant| %>
      <tr>
        <td> <%= participant.path %> </td>
        <td> <%= participant.user.name %> </td>
        <% for p in 1..@contest.problems_count %>
        <td></td>
        <% end %>
        <td>all</td>
        <td>penalty</td>
      </tr>
    <% end %>
  </tbody>
</table>